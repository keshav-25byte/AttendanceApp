# Dependencies
/node_modules
/.pnp
/.pnp.js

# Build artifacts
/dist
/web-build
/coverage
/build
/out

# Expo/React Native Specific
/.expo/
/expo-env.d.ts
/app-example
/tmp/
/temp/

# Environment Variables (Keep your secrets safe!)
.env
.env.local
.env.development
.env.production
# NOTE: .env.example is generally safe to commit, but the actual .env file should be kept secret.

# OS/IDE Artifacts
.DS_Store
Thumbs.db
*.orig
# Visual Studio Code settings (Optional - keep if you want to enforce editor settings for all users)
.vscode/

# Android-specific build outputs
android/build
android/.gradle
android/app/build
android/app/.cxx
# Keep the platform's own gitignore
!android/.gitignore

# iOS-specific build outputs
ios/build